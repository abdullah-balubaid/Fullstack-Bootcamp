// TODO (Step 3): Build the Header component
// It should display the app title as a large centered heading.
const Header = () => {
  return (
    <div className="text-6xl text-(--text-cream) font-lexend text-center font-lexend drop-shadow-[0_0_12px_var(--gold-cream)]">
      Arcade Mission List
    </div>
  );
};

export default Header;